services:
  bot:
    build: .
    restart: unless-stopped
    env_file: .env
    ports:
      - "8080:8080"   # вебхук amoJo (поставьте за reverse-proxy с HTTPS)
    volumes:
      - ./data:/app/data
